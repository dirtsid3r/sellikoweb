import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'Agents API - Coming soon' })
}

export async function POST() {
  return NextResponse.json({ message: 'Agents API - Coming soon' })
} 